<?xml version="1.0" encoding="utf-8"?>
<configuration>
    <startup> 
        <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.0,Profile=Client"/>
    </startup>
    <appSettings>
      <!-- This is the directory where NestorMSX will store its own state.
           If not an absolute path, it is relative to the folder of NestorMSX.exe -->    
      <!-- <add key="stateFileDirectory" value="$ApplicationData$/NestorMSX"/> -->
      <add key="stateFileDirectory" value=""/>
    </appSettings>
</configuration>
